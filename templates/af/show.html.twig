{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Like_number</th>
                <td>{{ avi.likeNumber }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ avi.rating }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('af_index') }}">back to list</a>

    <a href="{{ path('af_edit', {'id': avi.id}) }}">edit</a>

    {{ include('af/_delete_form.html.twig') }}
{% endblock %}
