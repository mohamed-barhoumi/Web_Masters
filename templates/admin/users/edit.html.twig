{% extends 'admin/base.html.twig' %}

{% block title %}<title>Edit User/Admin</title>{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card-box">
                <h4 class="m-t-0 header-title">Update/Delete User/Admin</h4>
                <div class="row">
                    <div class="col-12">
                        <div class="p-20">
                            {{ include('admin/users/_form.html.twig', {'button_label': 'Update'}) }}
                            <div class="p-1"></div>
                            {{ include('admin/users/_delete_form.html.twig') }}
                            <div class="p-1"></div>
                            <a class="btn btn-primary waves-effect waves-light" href="{{ path('user_back_index') }}" style="width: 100px">Back to list</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
